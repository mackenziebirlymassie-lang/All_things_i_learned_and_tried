'use strict';
const test = require('node:test');
const assert = require('node:assert/strict');
const { MetricsRegistry, Tracer, traced, IntegrityManifest, StateValidator, PluginVerifier, CapabilityPolicy, AuditLog, HealthRegistry, Sampler, RateLimiter, Envelope, Cursor, AccessPolicy, Principal, StateMachine } = require('../src');

test('observability/storage invariant 000', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 0});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-0', async span => { span.setAttribute('case', 0); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 0}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 0);
});
test('observability/storage invariant 001', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 1});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-1', async span => { span.setAttribute('case', 1); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 1}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 1);
});
test('observability/storage invariant 002', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 2});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-2', async span => { span.setAttribute('case', 2); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 2}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 2);
});
test('observability/storage invariant 003', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 3});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-3', async span => { span.setAttribute('case', 3); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 3}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 3);
});
test('observability/storage invariant 004', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 4});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-4', async span => { span.setAttribute('case', 4); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 4}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 4);
});
test('observability/storage invariant 005', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 5});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-5', async span => { span.setAttribute('case', 5); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 5}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 5);
});
test('observability/storage invariant 006', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 6});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-6', async span => { span.setAttribute('case', 6); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 6}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 6);
});
test('observability/storage invariant 007', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 7});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-7', async span => { span.setAttribute('case', 7); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 7}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 7);
});
test('observability/storage invariant 008', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 8});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-8', async span => { span.setAttribute('case', 8); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 8}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 8);
});
test('observability/storage invariant 009', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 9});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-9', async span => { span.setAttribute('case', 9); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 9}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 9);
});
test('observability/storage invariant 010', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 10});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-10', async span => { span.setAttribute('case', 10); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 10}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 10);
});
test('observability/storage invariant 011', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 11});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-11', async span => { span.setAttribute('case', 11); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 11}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 11);
});
test('observability/storage invariant 012', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 12});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-12', async span => { span.setAttribute('case', 12); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 12}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 12);
});
test('observability/storage invariant 013', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 13});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-13', async span => { span.setAttribute('case', 13); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 13}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 13);
});
test('observability/storage invariant 014', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 14});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-14', async span => { span.setAttribute('case', 14); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 14}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 14);
});
test('observability/storage invariant 015', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 15});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-15', async span => { span.setAttribute('case', 15); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 15}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 15);
});
test('observability/storage invariant 016', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 16});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-16', async span => { span.setAttribute('case', 16); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 16}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 16);
});
test('observability/storage invariant 017', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 17});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-17', async span => { span.setAttribute('case', 17); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 17}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 17);
});
test('observability/storage invariant 018', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 18});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-18', async span => { span.setAttribute('case', 18); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 18}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 18);
});
test('observability/storage invariant 019', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 19});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-19', async span => { span.setAttribute('case', 19); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 19}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 19);
});
test('observability/storage invariant 020', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 20});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-20', async span => { span.setAttribute('case', 20); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 20}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 20);
});
test('observability/storage invariant 021', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 21});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-21', async span => { span.setAttribute('case', 21); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 21}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 21);
});
test('observability/storage invariant 022', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 22});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-22', async span => { span.setAttribute('case', 22); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 22}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 22);
});
test('observability/storage invariant 023', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 23});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-23', async span => { span.setAttribute('case', 23); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 23}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 23);
});
test('observability/storage invariant 024', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 24});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-24', async span => { span.setAttribute('case', 24); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 24}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 24);
});
test('observability/storage invariant 025', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 25});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-25', async span => { span.setAttribute('case', 25); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 25}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 25);
});
test('observability/storage invariant 026', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 26});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-26', async span => { span.setAttribute('case', 26); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 26}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 26);
});
test('observability/storage invariant 027', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 27});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-27', async span => { span.setAttribute('case', 27); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 27}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 27);
});
test('observability/storage invariant 028', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 28});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-28', async span => { span.setAttribute('case', 28); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 28}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 28);
});
test('observability/storage invariant 029', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 29});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-29', async span => { span.setAttribute('case', 29); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 29}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 29);
});
test('observability/storage invariant 030', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 30});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-30', async span => { span.setAttribute('case', 30); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 30}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 30);
});
test('observability/storage invariant 031', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 31});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-31', async span => { span.setAttribute('case', 31); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 31}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 31);
});
test('observability/storage invariant 032', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 32});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-32', async span => { span.setAttribute('case', 32); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 32}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 32);
});
test('observability/storage invariant 033', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 33});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-33', async span => { span.setAttribute('case', 33); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 33}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 33);
});
test('observability/storage invariant 034', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 34});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-34', async span => { span.setAttribute('case', 34); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 34}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 34);
});
test('observability/storage invariant 035', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 35});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-35', async span => { span.setAttribute('case', 35); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 35}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 35);
});
test('observability/storage invariant 036', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 36});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-36', async span => { span.setAttribute('case', 36); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 36}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 36);
});
test('observability/storage invariant 037', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 37});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-37', async span => { span.setAttribute('case', 37); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 37}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 37);
});
test('observability/storage invariant 038', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 38});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-38', async span => { span.setAttribute('case', 38); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 38}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 38);
});
test('observability/storage invariant 039', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 39});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-39', async span => { span.setAttribute('case', 39); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 39}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 39);
});
test('observability/storage invariant 040', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 40});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-40', async span => { span.setAttribute('case', 40); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 40}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 40);
});
test('observability/storage invariant 041', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 41});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-41', async span => { span.setAttribute('case', 41); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 41}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 41);
});
test('observability/storage invariant 042', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 42});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-42', async span => { span.setAttribute('case', 42); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 42}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 42);
});
test('observability/storage invariant 043', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 43});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-43', async span => { span.setAttribute('case', 43); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 43}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 43);
});
test('observability/storage invariant 044', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 44});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-44', async span => { span.setAttribute('case', 44); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 44}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 44);
});
test('observability/storage invariant 045', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 45});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-45', async span => { span.setAttribute('case', 45); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 45}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 45);
});
test('observability/storage invariant 046', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 46});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-46', async span => { span.setAttribute('case', 46); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 46}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 46);
});
test('observability/storage invariant 047', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 47});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-47', async span => { span.setAttribute('case', 47); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 47}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 47);
});
test('observability/storage invariant 048', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 48});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-48', async span => { span.setAttribute('case', 48); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 48}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 48);
});
test('observability/storage invariant 049', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 49});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-49', async span => { span.setAttribute('case', 49); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 49}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 49);
});
test('observability/storage invariant 050', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 50});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-50', async span => { span.setAttribute('case', 50); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 50}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 50);
});
test('observability/storage invariant 051', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 51});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-51', async span => { span.setAttribute('case', 51); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 51}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 51);
});
test('observability/storage invariant 052', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 52});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-52', async span => { span.setAttribute('case', 52); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 52}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 52);
});
test('observability/storage invariant 053', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 53});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-53', async span => { span.setAttribute('case', 53); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 53}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 53);
});
test('observability/storage invariant 054', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 54});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-54', async span => { span.setAttribute('case', 54); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 54}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 54);
});
test('observability/storage invariant 055', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 55});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-55', async span => { span.setAttribute('case', 55); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 55}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 55);
});
test('observability/storage invariant 056', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 56});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-56', async span => { span.setAttribute('case', 56); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 56}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 56);
});
test('observability/storage invariant 057', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 57});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-57', async span => { span.setAttribute('case', 57); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 57}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 57);
});
test('observability/storage invariant 058', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 58});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-58', async span => { span.setAttribute('case', 58); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 58}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 58);
});
test('observability/storage invariant 059', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 59});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-59', async span => { span.setAttribute('case', 59); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 59}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 59);
});
test('observability/storage invariant 060', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 60});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-60', async span => { span.setAttribute('case', 60); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 60}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 60);
});
test('observability/storage invariant 061', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 61});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-61', async span => { span.setAttribute('case', 61); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 61}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 61);
});
test('observability/storage invariant 062', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 62});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-62', async span => { span.setAttribute('case', 62); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 62}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 62);
});
test('observability/storage invariant 063', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 63});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-63', async span => { span.setAttribute('case', 63); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 63}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 63);
});
test('observability/storage invariant 064', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 64});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-64', async span => { span.setAttribute('case', 64); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 64}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 64);
});
test('observability/storage invariant 065', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 65});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-65', async span => { span.setAttribute('case', 65); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 65}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 65);
});
test('observability/storage invariant 066', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 66});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-66', async span => { span.setAttribute('case', 66); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 66}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 66);
});
test('observability/storage invariant 067', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 67});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-67', async span => { span.setAttribute('case', 67); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 67}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 67);
});
test('observability/storage invariant 068', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 68});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-68', async span => { span.setAttribute('case', 68); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 68}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 68);
});
test('observability/storage invariant 069', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 69});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-69', async span => { span.setAttribute('case', 69); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 69}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 69);
});
test('observability/storage invariant 070', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 70});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-70', async span => { span.setAttribute('case', 70); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 70}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 70);
});
test('observability/storage invariant 071', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 71});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-71', async span => { span.setAttribute('case', 71); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 71}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 71);
});
test('observability/storage invariant 072', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 72});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-72', async span => { span.setAttribute('case', 72); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 72}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 72);
});
test('observability/storage invariant 073', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 73});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-73', async span => { span.setAttribute('case', 73); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 73}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 73);
});
test('observability/storage invariant 074', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 74});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-74', async span => { span.setAttribute('case', 74); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 74}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 74);
});
test('observability/storage invariant 075', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 75});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-75', async span => { span.setAttribute('case', 75); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 75}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 75);
});
test('observability/storage invariant 076', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 76});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-76', async span => { span.setAttribute('case', 76); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 76}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 76);
});
test('observability/storage invariant 077', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 77});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-77', async span => { span.setAttribute('case', 77); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 77}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 77);
});
test('observability/storage invariant 078', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 78});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-78', async span => { span.setAttribute('case', 78); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 78}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 78);
});
test('observability/storage invariant 079', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 79});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-79', async span => { span.setAttribute('case', 79); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 79}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 79);
});
test('observability/storage invariant 080', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 80});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-80', async span => { span.setAttribute('case', 80); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 80}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 80);
});
test('observability/storage invariant 081', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 81});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-81', async span => { span.setAttribute('case', 81); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 81}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 81);
});
test('observability/storage invariant 082', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 82});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-82', async span => { span.setAttribute('case', 82); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 82}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 82);
});
test('observability/storage invariant 083', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 83});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-83', async span => { span.setAttribute('case', 83); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 83}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 83);
});
test('observability/storage invariant 084', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 84});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-84', async span => { span.setAttribute('case', 84); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 84}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 84);
});
test('observability/storage invariant 085', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 85});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-85', async span => { span.setAttribute('case', 85); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 85}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 85);
});
test('observability/storage invariant 086', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 86});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-86', async span => { span.setAttribute('case', 86); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 86}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 86);
});
test('observability/storage invariant 087', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 87});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-87', async span => { span.setAttribute('case', 87); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 87}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 87);
});
test('observability/storage invariant 088', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 88});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-88', async span => { span.setAttribute('case', 88); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 88}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 88);
});
test('observability/storage invariant 089', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 89});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-89', async span => { span.setAttribute('case', 89); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 89}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 89);
});
test('observability/storage invariant 090', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 90});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-90', async span => { span.setAttribute('case', 90); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 90}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 90);
});
test('observability/storage invariant 091', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 91});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-91', async span => { span.setAttribute('case', 91); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 91}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 91);
});
test('observability/storage invariant 092', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 92});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-92', async span => { span.setAttribute('case', 92); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 92}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 92);
});
test('observability/storage invariant 093', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 93});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-93', async span => { span.setAttribute('case', 93); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 93}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 93);
});
test('observability/storage invariant 094', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 94});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-94', async span => { span.setAttribute('case', 94); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 94}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 94);
});
test('observability/storage invariant 095', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 95});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-95', async span => { span.setAttribute('case', 95); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 95}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 95);
});
test('observability/storage invariant 096', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 96});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-96', async span => { span.setAttribute('case', 96); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 96}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 96);
});
test('observability/storage invariant 097', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 97});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-97', async span => { span.setAttribute('case', 97); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 97}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 97);
});
test('observability/storage invariant 098', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 98});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-98', async span => { span.setAttribute('case', 98); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 98}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 98);
});
test('observability/storage invariant 099', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 99});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-99', async span => { span.setAttribute('case', 99); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 99}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 99);
});
test('observability/storage invariant 100', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 100});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-100', async span => { span.setAttribute('case', 100); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 100}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 100);
});
test('observability/storage invariant 101', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 101});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-101', async span => { span.setAttribute('case', 101); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 101}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 101);
});
test('observability/storage invariant 102', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 102});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-102', async span => { span.setAttribute('case', 102); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 102}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 102);
});
test('observability/storage invariant 103', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 103});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-103', async span => { span.setAttribute('case', 103); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 103}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 103);
});
test('observability/storage invariant 104', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 104});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-104', async span => { span.setAttribute('case', 104); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 104}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 104);
});
test('observability/storage invariant 105', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 105});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-105', async span => { span.setAttribute('case', 105); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 105}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 105);
});
test('observability/storage invariant 106', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 106});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-106', async span => { span.setAttribute('case', 106); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 106}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 106);
});
test('observability/storage invariant 107', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 107});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-107', async span => { span.setAttribute('case', 107); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 107}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 107);
});
test('observability/storage invariant 108', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 108});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-108', async span => { span.setAttribute('case', 108); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 108}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 108);
});
test('observability/storage invariant 109', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 109});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-109', async span => { span.setAttribute('case', 109); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 109}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 109);
});
test('observability/storage invariant 110', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 110});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-110', async span => { span.setAttribute('case', 110); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 110}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 110);
});
test('observability/storage invariant 111', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 111});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-111', async span => { span.setAttribute('case', 111); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 111}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 111);
});
test('observability/storage invariant 112', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 112});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-112', async span => { span.setAttribute('case', 112); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 112}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 112);
});
test('observability/storage invariant 113', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 113});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-113', async span => { span.setAttribute('case', 113); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 113}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 113);
});
test('observability/storage invariant 114', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 114});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-114', async span => { span.setAttribute('case', 114); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 114}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 114);
});
test('observability/storage invariant 115', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 115});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-115', async span => { span.setAttribute('case', 115); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 115}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 115);
});
test('observability/storage invariant 116', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 116});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-116', async span => { span.setAttribute('case', 116); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 116}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 116);
});
test('observability/storage invariant 117', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 117});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-117', async span => { span.setAttribute('case', 117); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 117}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 117);
});
test('observability/storage invariant 118', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 118});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-118', async span => { span.setAttribute('case', 118); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 118}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 118);
});
test('observability/storage invariant 119', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 119});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-119', async span => { span.setAttribute('case', 119); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 119}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 119);
});
test('observability/storage invariant 120', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 120});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-120', async span => { span.setAttribute('case', 120); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 120}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 120);
});
test('observability/storage invariant 121', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 121});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-121', async span => { span.setAttribute('case', 121); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 121}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 121);
});
test('observability/storage invariant 122', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 122});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-122', async span => { span.setAttribute('case', 122); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 122}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 122);
});
test('observability/storage invariant 123', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 123});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-123', async span => { span.setAttribute('case', 123); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 123}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 123);
});
test('observability/storage invariant 124', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 124});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-124', async span => { span.setAttribute('case', 124); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 124}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 124);
});
test('observability/storage invariant 125', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 125});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-125', async span => { span.setAttribute('case', 125); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 125}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 125);
});
test('observability/storage invariant 126', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 126});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-126', async span => { span.setAttribute('case', 126); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 126}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 126);
});
test('observability/storage invariant 127', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 127});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-127', async span => { span.setAttribute('case', 127); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 127}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 127);
});
test('observability/storage invariant 128', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 128});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-128', async span => { span.setAttribute('case', 128); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 128}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 128);
});
test('observability/storage invariant 129', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 129});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-129', async span => { span.setAttribute('case', 129); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 129}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 129);
});
test('observability/storage invariant 130', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 130});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-130', async span => { span.setAttribute('case', 130); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 130}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 130);
});
test('observability/storage invariant 131', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 131});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-131', async span => { span.setAttribute('case', 131); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 131}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 131);
});
test('observability/storage invariant 132', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 132});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-132', async span => { span.setAttribute('case', 132); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 132}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 132);
});
test('observability/storage invariant 133', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 133});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-133', async span => { span.setAttribute('case', 133); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 133}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 133);
});
test('observability/storage invariant 134', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 134});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-134', async span => { span.setAttribute('case', 134); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 134}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 134);
});
test('observability/storage invariant 135', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 135});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-135', async span => { span.setAttribute('case', 135); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 135}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 135);
});
test('observability/storage invariant 136', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 136});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-136', async span => { span.setAttribute('case', 136); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 136}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 136);
});
test('observability/storage invariant 137', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 137});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-137', async span => { span.setAttribute('case', 137); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 137}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 137);
});
test('observability/storage invariant 138', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 138});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-138', async span => { span.setAttribute('case', 138); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 138}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 138);
});
test('observability/storage invariant 139', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 139});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-139', async span => { span.setAttribute('case', 139); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 139}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 139);
});
test('observability/storage invariant 140', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 140});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-140', async span => { span.setAttribute('case', 140); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 140}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 140);
});
test('observability/storage invariant 141', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 141});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-141', async span => { span.setAttribute('case', 141); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 141}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 141);
});
test('observability/storage invariant 142', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 142});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-142', async span => { span.setAttribute('case', 142); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 142}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 142);
});
test('observability/storage invariant 143', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 143});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-143', async span => { span.setAttribute('case', 143); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 143}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 143);
});
test('observability/storage invariant 144', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 144});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-144', async span => { span.setAttribute('case', 144); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 144}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 144);
});
test('observability/storage invariant 145', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 145});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-145', async span => { span.setAttribute('case', 145); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 145}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 145);
});
test('observability/storage invariant 146', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 146});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-146', async span => { span.setAttribute('case', 146); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 146}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 146);
});
test('observability/storage invariant 147', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 147});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-147', async span => { span.setAttribute('case', 147); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 147}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 147);
});
test('observability/storage invariant 148', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 148});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-148', async span => { span.setAttribute('case', 148); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 148}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 148);
});
test('observability/storage invariant 149', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 149});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-149', async span => { span.setAttribute('case', 149); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 149}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 149);
});
test('observability/storage invariant 150', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 150});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-150', async span => { span.setAttribute('case', 150); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 150}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 150);
});
test('observability/storage invariant 151', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 151});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-151', async span => { span.setAttribute('case', 151); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 151}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 151);
});
test('observability/storage invariant 152', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 152});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-152', async span => { span.setAttribute('case', 152); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 152}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 152);
});
test('observability/storage invariant 153', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 153});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-153', async span => { span.setAttribute('case', 153); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 153}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 153);
});
test('observability/storage invariant 154', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 154});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-154', async span => { span.setAttribute('case', 154); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 154}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 154);
});
test('observability/storage invariant 155', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 155});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-155', async span => { span.setAttribute('case', 155); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 155}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 155);
});
test('observability/storage invariant 156', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 156});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-156', async span => { span.setAttribute('case', 156); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 156}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 156);
});
test('observability/storage invariant 157', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 157});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-157', async span => { span.setAttribute('case', 157); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 157}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 157);
});
test('observability/storage invariant 158', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 158});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-158', async span => { span.setAttribute('case', 158); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 158}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 158);
});
test('observability/storage invariant 159', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 159});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-159', async span => { span.setAttribute('case', 159); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 159}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 159);
});
test('observability/storage invariant 160', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 160});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-160', async span => { span.setAttribute('case', 160); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 160}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 160);
});
test('observability/storage invariant 161', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 161});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-161', async span => { span.setAttribute('case', 161); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 161}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 161);
});
test('observability/storage invariant 162', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 162});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-162', async span => { span.setAttribute('case', 162); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 162}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 162);
});
test('observability/storage invariant 163', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 163});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-163', async span => { span.setAttribute('case', 163); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 163}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 163);
});
test('observability/storage invariant 164', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 164});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-164', async span => { span.setAttribute('case', 164); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 164}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 164);
});
test('observability/storage invariant 165', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 165});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-165', async span => { span.setAttribute('case', 165); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 165}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 165);
});
test('observability/storage invariant 166', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 166});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-166', async span => { span.setAttribute('case', 166); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 166}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 166);
});
test('observability/storage invariant 167', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 167});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-167', async span => { span.setAttribute('case', 167); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 167}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 167);
});
test('observability/storage invariant 168', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 168});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-168', async span => { span.setAttribute('case', 168); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 168}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 168);
});
test('observability/storage invariant 169', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 169});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-169', async span => { span.setAttribute('case', 169); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 169}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 169);
});
test('observability/storage invariant 170', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 170});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-170', async span => { span.setAttribute('case', 170); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 170}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 170);
});
test('observability/storage invariant 171', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 171});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-171', async span => { span.setAttribute('case', 171); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 171}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 171);
});
test('observability/storage invariant 172', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 172});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-172', async span => { span.setAttribute('case', 172); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 172}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 172);
});
test('observability/storage invariant 173', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 173});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-173', async span => { span.setAttribute('case', 173); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 173}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 173);
});
test('observability/storage invariant 174', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 174});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-174', async span => { span.setAttribute('case', 174); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 174}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 174);
});
test('observability/storage invariant 175', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 175});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-175', async span => { span.setAttribute('case', 175); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 175}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 175);
});
test('observability/storage invariant 176', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 176});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-176', async span => { span.setAttribute('case', 176); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 176}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 176);
});
test('observability/storage invariant 177', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 177});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-177', async span => { span.setAttribute('case', 177); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 177}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 177);
});
test('observability/storage invariant 178', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 178});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-178', async span => { span.setAttribute('case', 178); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 178}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 178);
});
test('observability/storage invariant 179', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 179});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-179', async span => { span.setAttribute('case', 179); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 179}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 179);
});
test('observability/storage invariant 180', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 180});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-180', async span => { span.setAttribute('case', 180); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 180}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 180);
});
test('observability/storage invariant 181', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 181});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-181', async span => { span.setAttribute('case', 181); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 181}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 181);
});
test('observability/storage invariant 182', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 182});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-182', async span => { span.setAttribute('case', 182); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 182}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 182);
});
test('observability/storage invariant 183', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 183});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-183', async span => { span.setAttribute('case', 183); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 183}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 183);
});
test('observability/storage invariant 184', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 184});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-184', async span => { span.setAttribute('case', 184); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 184}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 184);
});
test('observability/storage invariant 185', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 185});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-185', async span => { span.setAttribute('case', 185); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 185}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 185);
});
test('observability/storage invariant 186', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 186});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-186', async span => { span.setAttribute('case', 186); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 186}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 186);
});
test('observability/storage invariant 187', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 187});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-187', async span => { span.setAttribute('case', 187); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 187}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 187);
});
test('observability/storage invariant 188', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 188});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-188', async span => { span.setAttribute('case', 188); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 188}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 188);
});
test('observability/storage invariant 189', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 189});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-189', async span => { span.setAttribute('case', 189); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 189}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 189);
});
test('observability/storage invariant 190', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 190});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-190', async span => { span.setAttribute('case', 190); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 190}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 190);
});
test('observability/storage invariant 191', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 191});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-191', async span => { span.setAttribute('case', 191); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 191}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 191);
});
test('observability/storage invariant 192', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 192});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-192', async span => { span.setAttribute('case', 192); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 192}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 192);
});
test('observability/storage invariant 193', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 193});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-193', async span => { span.setAttribute('case', 193); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 193}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 193);
});
test('observability/storage invariant 194', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 194});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-194', async span => { span.setAttribute('case', 194); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 194}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 194);
});
test('observability/storage invariant 195', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 195});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-195', async span => { span.setAttribute('case', 195); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 195}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 195);
});
test('observability/storage invariant 196', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 196});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-196', async span => { span.setAttribute('case', 196); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 196}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 196);
});
test('observability/storage invariant 197', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 197});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-197', async span => { span.setAttribute('case', 197); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 197}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 197);
});
test('observability/storage invariant 198', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 198});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-198', async span => { span.setAttribute('case', 198); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 198}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 198);
});
test('observability/storage invariant 199', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 199});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-199', async span => { span.setAttribute('case', 199); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 199}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 199);
});
test('observability/storage invariant 200', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 200});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-200', async span => { span.setAttribute('case', 200); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 200}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 200);
});
test('observability/storage invariant 201', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 201});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-201', async span => { span.setAttribute('case', 201); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 201}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 201);
});
test('observability/storage invariant 202', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 202});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-202', async span => { span.setAttribute('case', 202); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 202}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 202);
});
test('observability/storage invariant 203', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 203});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-203', async span => { span.setAttribute('case', 203); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 203}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 203);
});
test('observability/storage invariant 204', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 204});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-204', async span => { span.setAttribute('case', 204); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 204}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 204);
});
test('observability/storage invariant 205', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 205});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-205', async span => { span.setAttribute('case', 205); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 205}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 205);
});
test('observability/storage invariant 206', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 206});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-206', async span => { span.setAttribute('case', 206); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 206}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 206);
});
test('observability/storage invariant 207', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 207});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-207', async span => { span.setAttribute('case', 207); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 207}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 207);
});
test('observability/storage invariant 208', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 208});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-208', async span => { span.setAttribute('case', 208); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 208}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 208);
});
test('observability/storage invariant 209', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 209});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-209', async span => { span.setAttribute('case', 209); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 209}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 209);
});
test('observability/storage invariant 210', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 210});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-210', async span => { span.setAttribute('case', 210); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 210}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 210);
});
test('observability/storage invariant 211', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 211});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-211', async span => { span.setAttribute('case', 211); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 211}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 211);
});
test('observability/storage invariant 212', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 212});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-212', async span => { span.setAttribute('case', 212); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 212}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 212);
});
test('observability/storage invariant 213', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 213});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-213', async span => { span.setAttribute('case', 213); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 213}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 213);
});
test('observability/storage invariant 214', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 214});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-214', async span => { span.setAttribute('case', 214); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 214}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 214);
});
test('observability/storage invariant 215', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 215});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-215', async span => { span.setAttribute('case', 215); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 215}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 215);
});
test('observability/storage invariant 216', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 216});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-216', async span => { span.setAttribute('case', 216); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 216}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 216);
});
test('observability/storage invariant 217', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 217});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-217', async span => { span.setAttribute('case', 217); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 217}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 217);
});
test('observability/storage invariant 218', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 218});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-218', async span => { span.setAttribute('case', 218); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 218}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 218);
});
test('observability/storage invariant 219', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 219});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-219', async span => { span.setAttribute('case', 219); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 219}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 219);
});
test('observability/storage invariant 220', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 220});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-220', async span => { span.setAttribute('case', 220); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 220}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 220);
});
test('observability/storage invariant 221', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 221});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-221', async span => { span.setAttribute('case', 221); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 221}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 221);
});
test('observability/storage invariant 222', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 222});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-222', async span => { span.setAttribute('case', 222); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 222}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 222);
});
test('observability/storage invariant 223', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 223});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-223', async span => { span.setAttribute('case', 223); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 223}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 223);
});
test('observability/storage invariant 224', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 224});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-224', async span => { span.setAttribute('case', 224); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 224}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 224);
});
test('observability/storage invariant 225', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 225});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-225', async span => { span.setAttribute('case', 225); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 225}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 225);
});
test('observability/storage invariant 226', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 226});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-226', async span => { span.setAttribute('case', 226); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 226}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 226);
});
test('observability/storage invariant 227', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 227});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-227', async span => { span.setAttribute('case', 227); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 227}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 227);
});
test('observability/storage invariant 228', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 228});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-228', async span => { span.setAttribute('case', 228); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 228}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 228);
});
test('observability/storage invariant 229', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 229});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-229', async span => { span.setAttribute('case', 229); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 229}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 229);
});
test('observability/storage invariant 230', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 230});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-230', async span => { span.setAttribute('case', 230); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 230}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 230);
});
test('observability/storage invariant 231', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 231});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-231', async span => { span.setAttribute('case', 231); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 231}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 231);
});
test('observability/storage invariant 232', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 232});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-232', async span => { span.setAttribute('case', 232); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 232}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 232);
});
test('observability/storage invariant 233', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 233});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-233', async span => { span.setAttribute('case', 233); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 233}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 233);
});
test('observability/storage invariant 234', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 234});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-234', async span => { span.setAttribute('case', 234); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 234}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 234);
});
test('observability/storage invariant 235', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 235});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-235', async span => { span.setAttribute('case', 235); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 235}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 235);
});
test('observability/storage invariant 236', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 236});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-236', async span => { span.setAttribute('case', 236); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 236}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 236);
});
test('observability/storage invariant 237', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 237});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-237', async span => { span.setAttribute('case', 237); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 237}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 237);
});
test('observability/storage invariant 238', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 238});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-238', async span => { span.setAttribute('case', 238); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 238}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 238);
});
test('observability/storage invariant 239', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 239});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-239', async span => { span.setAttribute('case', 239); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 239}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 239);
});
test('observability/storage invariant 240', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 240});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-240', async span => { span.setAttribute('case', 240); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 240}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 240);
});
test('observability/storage invariant 241', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 241});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-241', async span => { span.setAttribute('case', 241); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 241}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 241);
});
test('observability/storage invariant 242', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 242});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-242', async span => { span.setAttribute('case', 242); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 242}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 242);
});
test('observability/storage invariant 243', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 243});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-243', async span => { span.setAttribute('case', 243); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 243}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 243);
});
test('observability/storage invariant 244', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 244});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-244', async span => { span.setAttribute('case', 244); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 244}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 244);
});
test('observability/storage invariant 245', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 245});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-245', async span => { span.setAttribute('case', 245); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 245}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 245);
});
test('observability/storage invariant 246', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 246});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-246', async span => { span.setAttribute('case', 246); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 246}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 246);
});
test('observability/storage invariant 247', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 247});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-247', async span => { span.setAttribute('case', 247); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 247}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 247);
});
test('observability/storage invariant 248', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 248});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-248', async span => { span.setAttribute('case', 248); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 248}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 248);
});
test('observability/storage invariant 249', async () => {
  const metrics = new MetricsRegistry();
  metrics.counter('operations').inc({case: 249});
  assert.equal(metrics.collect().counters.length, 1);
  const tracer = new Tracer();
  await traced(tracer, 'operation-249', async span => { span.setAttribute('case', 249); return true; });
  assert.equal(tracer.summary().spans, 1);
  const envelope = new Envelope({type: 'case', payload: {index: 249}}).sign('secret');
  assert.equal(envelope.verify('secret'), true);
  const decoded = Envelope.parse(envelope.serialize());
  assert.equal(decoded.payload.index, 249);
});
test('plugin security invariant 000', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-0', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 001', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-1', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 002', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-2', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 003', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-3', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 004', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-4', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 005', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-5', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 006', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-6', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 007', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-7', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 008', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-8', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 009', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-9', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 010', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-10', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 011', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-11', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 012', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-12', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 013', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-13', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 014', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-14', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 015', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-15', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 016', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-16', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 017', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-17', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 018', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-18', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 019', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-19', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 020', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-20', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 021', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-21', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 022', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-22', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 023', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-23', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 024', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-24', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 025', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-25', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 026', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-26', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 027', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-27', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 028', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-28', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 029', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-29', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 030', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-30', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 031', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-31', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 032', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-32', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 033', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-33', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 034', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-34', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 035', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-35', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 036', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-36', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 037', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-37', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 038', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-38', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 039', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-39', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 040', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-40', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 041', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-41', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 042', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-42', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 043', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-43', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 044', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-44', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 045', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-45', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 046', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-46', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 047', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-47', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 048', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-48', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 049', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-49', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 050', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-50', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 051', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-51', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 052', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-52', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 053', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-53', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 054', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-54', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 055', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-55', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 056', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-56', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 057', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-57', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 058', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-58', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 059', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-59', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 060', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-60', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 061', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-61', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 062', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-62', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 063', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-63', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 064', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-64', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 065', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-65', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 066', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-66', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 067', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-67', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 068', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-68', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 069', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-69', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 070', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-70', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 071', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-71', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 072', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-72', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 073', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-73', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 074', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-74', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 075', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-75', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 076', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-76', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 077', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-77', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 078', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-78', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 079', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-79', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 080', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-80', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 081', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-81', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 082', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-82', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 083', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-83', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 084', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-84', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 085', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-85', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 086', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-86', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 087', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-87', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 088', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-88', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 089', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-89', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 090', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-90', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 091', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-91', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 092', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-92', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 093', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-93', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 094', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-94', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 095', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-95', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 096', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-96', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 097', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-97', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 098', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-98', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('plugin security invariant 099', () => {
  const policy = new CapabilityPolicy({allowed: ['commands', 'handlers']});
  assert.equal(policy.check('commands'), true);
  assert.throws(() => policy.check('process'), /denied/);
  const verifier = new PluginVerifier({secret: 'secret', requireSignature: true});
  const manifest = {name: 'plugin-99', version: '1.0.0', capabilities: ['commands']};
  const {signManifest} = require('../src');
  manifest.signature = signManifest(manifest, 'secret');
  assert.equal(verifier.verify(manifest), true);
  const audit = new AuditLog(); audit.record('verify', manifest);
  assert.equal(audit.query({action: 'verify'}).length, 1);
});
test('state and health invariant 000', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 001', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 002', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 003', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 004', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 005', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 006', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 007', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 008', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 009', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 010', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 011', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 012', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 013', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 014', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 015', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 016', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 017', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 018', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 019', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 020', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 021', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 022', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 023', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 024', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 025', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 026', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 027', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 028', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 029', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 030', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 031', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 032', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 033', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 034', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 035', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 036', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 037', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 038', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 039', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 040', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 041', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 042', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 043', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 044', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 045', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 046', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 047', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 048', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 049', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 050', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 051', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 052', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 053', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 054', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 055', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 056', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 057', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 058', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 059', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 060', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 061', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 062', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 063', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 064', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 065', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 066', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 067', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 068', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 069', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 070', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 071', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 072', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 073', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 074', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 075', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 076', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 077', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 078', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 079', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 080', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 081', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 082', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 083', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 084', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 085', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 086', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 087', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 088', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 089', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 090', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 091', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 092', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 093', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 094', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 095', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 096', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 097', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 098', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
test('state and health invariant 099', async () => {
  const state = new StateMachine({name: 'case', initial: 'idle'});
  state.add({event: 'start', from: 'idle', to: 'running'}).add({event: 'stop', from: 'running', to: 'done'});
  await state.send('start'); await state.send('stop');
  assert.equal(state.state, 'done');
  const health = new HealthRegistry();
  health.register('always', () => true);
  const result = await health.run({force: true});
  assert.equal(result.ok, true);
  const limiter = new RateLimiter({limit: 2});
  assert.equal(limiter.allow(), true); assert.equal(limiter.remaining() >= 0, true);
});
